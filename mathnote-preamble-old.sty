\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{mathnote-preamble}[2024/11/13 Math note modern preamble]

\RequirePackage{iftex}
% ?????? XeLaTeX / LuaLaTeX???????? pdfTeX ??????????????????????
\ifPDFTeX
  \PackageError{mathnote-preamble}{?????\XeLaTeX\ ?? LuaLaTeX ????????}{?? MikTeX / TeX Live ???§Ý??? \XeLaTeX\ ?? LuaLaTeX ???¡À???}
\fi

\makeatletter
% ????? mathnote-* ????????????¡¤???????????????????
\newif\ifmathnote@rootfound
\mathnote@rootfoundfalse
\def\mathnote@rootpath{}
\newcommand*\MathNote@TestRoot[1]{%
  \IfFileExists{#1mathnote-core.tex}{%
    \gdef\mathnote@rootpath{#1}%
    \mathnote@rootfoundtrue
  }{}%
}
\MathNote@TestRoot{}
\ifmathnote@rootfound\else\MathNote@TestRoot{../}\fi
\ifmathnote@rootfound\else\MathNote@TestRoot{../../}\fi
\ifmathnote@rootfound\else\MathNote@TestRoot{../../../}\fi
\ifmathnote@rootfound\else\MathNote@TestRoot{../../../../}\fi
\ifmathnote@rootfound\else\MathNote@TestRoot{../../../../../}\fi
\ifmathnote@rootfound\else
  \PackageError{mathnote-preamble}{mathnote-core.tex ¦Ä???}{????? mathnote ?????????????????¡¤???????????}
\fi
\newcommand*\MathNoteInputFromRoot[1]{%
  \input{\mathnote@rootpath#1}%
}

% ?????????????????????????????????????
\MathNoteInputFromRoot{mathnote-core.tex}

% ????????? / ?????????????????
\MathNoteInputFromRoot{mathnote-colors.tex}

% ??????????????????¨¹???
\MathNoteInputFromRoot{mathnote-structure.tex}

% ????????????????????????????????¨´???
\MathNoteInputFromRoot{mathnote-content.tex}

% ?·Ú????ï“?????§Ö???·Ú | ?????????
% ???????? \noteversion | \notedate??
% ???????????? \noteversionmark?????????????????
\providecommand{\noteversionmark}{}
\newcommand{\MathNoteVersionBlock}{%
  {\normalsize\color{inkgray}%
    \if\relax\detokenize{\noteversionmark}\relax
      \noteversion\quad|\quad\notedate
    \else
      \noteversionmark
    \fi
    \par
  }%
}

\makeatother

