# 数学笔记 XeLaTeX 模板使用说明

本模板基于 `mathnote-preamble.tex` 导言区构建，提供统一字体与配色、彩色数学盒子、TikZ 样式以及屏幕 / 印刷双通道支持，用于编写结构清晰的数学学习笔记。

## 1. 编译方式

- 推荐使用 XeLaTeX 编译（或 LuaLaTeX）：
  ```bash
  xelatex 数学笔记模板.tex
  xelatex 数学笔记模板.tex  # 建议编译两次以刷新目录与交叉引用
  ```
- 若使用 pdfLaTeX 会直接报错并提示切换引擎。

## 2. 文档类与页面设置

- 文档类：`ctexart`，自动支持中文。
- 页面：A4 纸张，页边距约为：上下 2.4–2.5 cm，左右 2.0–2.1 cm。
- 字号：默认 `11pt`，如需修改，可在文档类选项中调整：
  ```latex
  \documentclass[a4paper,12pt]{ctexart}
  ```

## 3. 颜色主题

模板内置现代低饱和度配色，并兼顾屏幕与 CMYK 输出：

- `accent`：主色，用于章节标题、定理盒、关键标记。
- `secondary`：次强调色，用于定义、辅助说明。
- `highlight`：高光色，用于示例、提示。
- `inkgray`：中性文字色，用于页眉、注释。
- `surface`：浅色背景，提高对比度。

可通过 `\colorlet` 或 `\definecolor` 覆盖这些颜色，然后执行 `\MathNoteRefreshColors` 让盒子与链接同步新配色。

## 4. 屏幕 / 印刷模式切换

- 默认采用 sRGB 配色，亮度友好，适合屏幕阅读。
- 若主要面向印刷输出，可在导言中（`\input{mathnote-preamble.tex}` 之后、`\begin{document}` 之前）调用：
  ```latex
  \MathNoteEnablePrint
  ```
  该命令会启用 CMYK 友好配色并将超链接改为 `hidelinks`。

## 5. 元信息与页眉页脚

在导言区通过以下命令配置笔记级元信息（作用于 PDF 书签与页眉 / 页脚）:

```latex
\renewcommand{\notetitle}{数学笔记标题}
\renewcommand{\noteauthor}{作者姓名}
\renewcommand{\notedate}{\today}
\renewcommand{\notesubtitle}{可选副标题}
\renewcommand{\noteversion}{v1.0}  % 当前版本号
```

- 页眉左侧显示整本笔记标题，右侧为当前章节（`标题 > 节名`）。
- 页脚显示“当前页 / 总页数”。

### 5.1 封面版本标记（多行）

在示例文件 `数学笔记模板.tex` 中，封面右下角的版本区域由 `\MathNoteVersionBlock` 绘制：

- 默认行为：当未设置 `\noteversionmark` 时，封面显示单行
  ```text
  <noteversion> | <notedate>
  ```
  即 `\noteversion | \notedate`。
- 多行版本记录：若你希望记录多个版本，例如
  ```text
  第一版|2025年01月01日
  第二版|2025年03月01日
  ```
  可在导言区添加
  ```latex
  \renewcommand{\noteversionmark}{%
    第一版\quad|\quad2025年01月01日\\
    第二版\quad|\quad2025年03月01日
  }
  ```
- 规则：当 `\noteversionmark` 非空时，封面会优先显示这里的多行内容；`\noteversion` 与 `\notedate` 仍可表示“当前版本 + 日期”，供元信息或页眉使用。

示例（接近你想要的效果）:
```latex
\renewcommand{\noteversionmark}{%
  第一版\quad|\quad2025年13月32日\\
  第二版\quad|\quad2025年13月32日
}
```

## 6. 盒子环境与信息层级

模板提供一组基于 `tcolorbox` 的彩色盒子，支持自动分页：

- 定义：
  ```latex
  \begin{definitionbox}{定义标题}
  内容...
  \end{definitionbox}
  ```
- 定理：
  ```latex
  \begin{theorembox}{定理标题}
  内容...
  \end{theorembox}
  ```
- 例题 / 技巧：`examplebox`
- 引理：`lemmabox`
- 注意：`notebox`
- 总结：`summarybox`

所有盒子默认是 `breakable`，不会被页边缘截断，左侧色条与细线框保证视觉层级统一。

辅助命令：

- `\keyword{关键字}`：以主题色加粗关键短语。
- `\inlinehint{文字}`：小号无衬线提示，适合边注。
- `focuspoints` 环境：高亮关键检查点。
- `\PageTag{文本}`：在当前页右上角添加浮动标签，标记专题或阶段。

## 7. 数学环境与符号

模板集成常用数学命令（基于 `mathtools` / `amsmath`）：

- 数域与集合：`\R`、`\C`、`\Q`、`\Z`、`\N`。
- 括号对：`\abs{z}`、`\norm{\mathbf{v}}`、`\ceil{x}`、`\floor{x}` 等支持星号形式自动调节大小。
- 导数记号：`\dd`、`\ee`、`\dv{f}{x}`、`\pdv{f}{x}`。

可直接在公式中使用，无需重复声明。

## 8. TikZ 绘图与代码块

- 已预加载 TikZ 常用库：`calc`、`arrows.meta`、`decorations.pathmorphing`、`positioning` 等，并提供 `mathnote lines` 与 `mathnote grid` 两种预设样式用于函数图像或网格背景。
- 代码块使用 `listings`，默认浅灰背景与主题色关键字。可通过
  ```latex
  \lstset{language=Python}
  ```
  或在局部 `lstlisting` 环境中设定具体语言。

## 9. 基本使用示例

```latex
\documentclass[a4paper,11pt]{ctexart}
\input{mathnote-preamble.tex}
% \MathNoteEnablePrint  % 若主要面向印刷，可解除注释

\renewcommand{\notetitle}{数学笔记标题}
\renewcommand{\noteauthor}{你的名字}
\renewcommand{\notedate}{2025-01-01}
\renewcommand{\notesubtitle}{副标题示例}
\renewcommand{\noteversion}{v1.0}
% 可选：多行版本标记
% \renewcommand{\noteversionmark}{第一版|2025年01月01日\\第二版|2025年03月01日}

\begin{document}

\tableofcontents
\newpage

\section{章节标题}
\subsection{小节标题}

\begin{definitionbox}{定义标题}
定义内容...
\end{definitionbox}

\begin{theorembox}{定理标题}
定理内容...
\end{theorembox}

\end{document}
```

## 10. 自定义与常见问题

- **修改颜色**：在导言中覆盖 `accent`、`secondary` 等颜色后调用 `\MathNoteRefreshColors` 即可同步更新盒子与链接。
- **修改页边距**：通过 `geometry`，例如
  ```latex
  \geometry{left=2cm,right=2cm,top=2.5cm,bottom=2.5cm}
  ```
- **编译报字体错误**：确认已安装对应中文字体，或通过 `\MathNoteFontDir` 指向本地字体目录。
- **目录页不刷新**：多编译几次（至少两次）以更新 `.toc` 与交叉引用。

如需更细致的配置和说明，可结合 `mathnote-preamble-guide.md` 一起阅读与修改。 
